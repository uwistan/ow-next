export default function HomePage() {
  // Content is rendered by the layout's ViewTransition (CreateView / LibraryView)
  // This page component exists to register the "/" route under the (app) layout
  return null;
}
